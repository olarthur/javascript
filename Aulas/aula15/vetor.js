let v = [2, 4, 5, 7, 9, 3]

//Mostrar vetor em tela simplificado
for (let p in v) {
    console.log(`A posição ${p} tem o valor ${v[p]}`)
}

/*
for (let p = 0; p < v.length; p++) {
    console.log(`A posição ${p} tem o valor ${v[p]}`)
}
*/